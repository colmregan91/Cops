using System.IO;
using UnityEngine;
public class LoadProgress : MonoBehaviour
{
    public void Load()
    {
        //if (File.Exists(Application.persistentDataPath + "/save.txt"))
        //{

        //    var r = File.ReadAllText(Application.persistentDataPath + "/save.txt");
        //    var w = JsonUtility.FromJson<SaveProgress>(r);
        //    Debug.Log(w.name);
        //}


  
    }

}
